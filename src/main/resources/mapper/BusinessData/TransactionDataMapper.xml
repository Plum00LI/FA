<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssaw.BusinessData.mapper.TransactionDataMapper">

    <insert id="insertTransactionData" parameterType="com.ssaw.BusinessData.entity.TransactionData">
        insert into transactionData (tradeId,dateTime,settlementDate,fundId,securitiesId,brokersId,seateId,transactionDataMode,
        status,price,num,accountId,netReceipts,totalSum,flag,commission,transfer,brokerage,stamp,management,security,transactionDataDesc)
        values (#{tradeId},#{dateTime},#{settlementDate},#{fundId},#{securitiesId},#{brokersId},#{seateId},#{transactionDataMode},#{status},
        #{price},#{num},#{accountId},#{netReceipts},#{totalSum},#{flag},#{commission},#{transfer},#{brokerage},
        #{stamp},#{management},#{security},#{transactionDataDesc})
    </insert>
    <update id="updateTransactionData" parameterType="com.ssaw.BusinessData.entity.TransactionData">
        update transactionData set dateTime=#{dateTime},settlementDate=#{settlementDate},fundId=#{fundId},
        securitiesId=#{securitiesId},brokersId=#{brokersId},seateId=#{seateId},transactionDataMode=#{transactionDataMode},status=#{status},
        price=#{price},num=#{num},accountId=#{accountId},netReceipts=#{netReceipts},totalSum=#{totalSum},flag=#{flag},
        commission=#{commission},transfer=#{transfer},brokerage=#{brokerage}, stamp=#{stamp},management=#{management},
        security=#{security},transactionDataDesc=#{transactionDataDesc}
        where tradeId=#{tradeId}
    </update>
    <delete id="deleteTransactionData" parameterType="string">
        delete from transactionData WHERE tradeId=#{tradeId}
    </delete>
    <select id="selectTransactionData" resultType="com.ssaw.BusinessData.entity.TransactionData">
        select tradeId,dateTime,settlementDate,fundId,securitiesId,brokersId,seateId,transactionDataMode,
        status,price,num,accountId,netReceipts,totalSum,flag,commission,transfer,brokerage,stamp,management,security,transactionDataDesc
        from transactionData
    </select>
</mapper>