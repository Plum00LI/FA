<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssaw.BusinessDescription.mapper.BondMapper">
    <!--增加-->
    <insert id="insertBond">
        insert into Bond(securitiesId,bondName,drawStartDate,drawEndDate,bondType,parRate,bondRate,bondRateAmount,payInterestNum,bonddesc) values
        (#{securitiesId},#{bondName},#{drawStartDate},#{drawEndDate},#{bondType},#{parRate},#{bondRateAmount},#{payInterestNum},#{bonddesc})
    </insert>
    <!--修改-->
    <update id="deleteBond">
        update securitiesId set bondName=#{bondName},drawStartDate=#{drawStartDate},drawEndDate=#{drawEndDate},bondType=#{bondType},
        parRate=#{parRate},bondRate#{bondRate},bondRateAmount=#{bondRateAmount},payInterestNum=#{payInterestNum},bonddesc=#{bonddesc}
    </update>
    <!--删除-->
    <delete id="updateBond">  delete from Bond where securitiesId=#{securitiesId}</delete>
    <!--查询-->
    <select id="selectBond"  resultType="com.ssaw.TAManagement.entity.TaTransaction">
         select * from Bond
    </select>

</mapper>